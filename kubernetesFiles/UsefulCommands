See token for dashboard:
token=$(sudo microk8s.kubectl -n kube-system get secret | grep default-token | cut -d " " -f1)
sudo microk8s.kubectl -n kube-system describe secret $token


See ip dashboard:
sudo microk8s.kubectl get services --A

Delete deployment:
sudo microk8s.kubectl delete -n default deployment bb-demo

Delete service:
sudo microk8s.kubectl delete -n default service bb-entrypoint

Debuging pods:
sudo microk8s.kubectl describe pod <pod-id>


